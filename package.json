{
  "scripts": {
    "gulp": "gulp",
    "generate-component": "gulp generate-component",
    "generate-project": "gulp clone-repo"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-git": "^2.10.1",
    "gulp-prompt": "^1.2.0",
    "gulp-rename": "^2.0.0",
    "gulp-replace": "^1.1.3",
    "gulp-uglify": "^3.0.2"
  }
}
